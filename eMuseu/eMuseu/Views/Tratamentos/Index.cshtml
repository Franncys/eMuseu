@model IEnumerable<eMuseu.Models.Tratamentos>

@{
    ViewBag.Title = "Index";
}

<h2>Tratamentos</h2>

<p>
    @Html.ActionLink("Novo Tratamento", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeTratamento)
        </th>
        <th>
            
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeTratamento)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.TratamentoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TratamentoID })
        </td>
    </tr>
}

</table>
